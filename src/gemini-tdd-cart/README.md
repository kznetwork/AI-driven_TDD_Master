# ASK(RED) To-Do

> RED 원칙: 구현 없이 실패 테스트만 · `tests/`만 수정 · 통과시키지 않는다.

- [ ] INV-1 — 상품 합계 66,000원에 문턱 할인 10%를 적용하면 할인액 6,600원과 할인 후 금액 59,400원이 되는지 확인한다. (파일: `tests/entity/test_cart.py`, Track: B)
- [ ] INV-2 — VIP 주문의 문턱 할인 후 금액에 VIP 할인 5%가 추가 적용되는지 확인한다. (파일: `tests/entity/test_cart.py`, Track: B)
- [ ] INV-3 — 계산 진입 경로와 관계없이 문턱 할인 후 VIP 할인 순서가 유지되는지 확인한다. (파일: `tests/entity/test_cart.py`, Track: B)
- [ ] E-1 — 수량 -1을 제출하면 정상 합계 0원 대신 오류가 표시되는지 확인한다. (파일: `tests/boundary/test_order_form.py`, Track: A)
- [ ] E-2 — `items`가 없는 주문을 제출하면 500이 아닌 통제된 오류가 반환되는지 확인한다. (파일: `tests/boundary/test_order_form.py`, Track: A)
- [ ] E-3 — 정확히 50,000원인 경계에 할인 적용 또는 미적용을 단언하는 테스트가 없는지 확인한다. (파일: `tests/entity/test_cart.py`, Track: B)
- [ ] AC-1 — **Given** A 12,000원 3개와 B 30,000원 1개가 주어지고 **When** 할인 금액을 계산하면 **Then** 합계는 66,000원이고 문턱 할인 후 금액은 59,400원이어야 한다.
- [ ] AC-2 — **Given** 문턱 할인과 VIP 할인이 모두 적용되고 **When** 최종 금액을 계산하면 **Then** 문턱 할인 후 금액에 VIP 할인 5%가 적용되어야 한다.
- [ ] AC-3 — **Given** 수량 -1이 입력되고 **When** 주문을 계산하거나 제출하면 **Then** 합계 0원의 정상 주문이 아니라 오류로 처리되어야 한다.
- [ ] AC-4 — **Given** 요청에 `items`가 없고 **When** 주문을 제출하면 **Then** 500이 아닌 통제된 오류가 반환되어야 한다.
- [ ] 골든셋 기준선 생성 (파일: `tests/fixtures/cart_golden.json`)

## 릴리즈 정보

| 항목 | 값 |
|---|---|
| 버전 (SemVer) | |
| 날짜 | |
| 이번 사이클 범위 (구현할 계약 ID) | |

**OOS(구현 금지):** 쿠폰·배송비 계산, 정확히 50,000원의 할인 적용 여부, 구체적인 오류 형식과 상태 코드, 계산 성능 목표는 확인 전까지 구현하지 않는다.

**완료 기준(DoD):** 전 계약 GREEN · 골든셋 회귀 통과 · 추적성 매트릭스 빈칸 없음(PRD 6절)

### 변경 이력(Changelog)

| 버전 | 날짜 | 요약 |
|---|---|---|
|  |  |  |
